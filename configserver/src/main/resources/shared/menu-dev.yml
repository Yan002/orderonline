server:
  port: 8020
spring:
  application:
    name: menu
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/orderingsystem?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: aptx4869
    name: orderingsystem

mybatis:
  mapper-locations: classpath:/mapping/*.xml
  type-aliases-package: com.yan.entity #指定entity扫描包类让mybatis自定扫描到自定义的entity

